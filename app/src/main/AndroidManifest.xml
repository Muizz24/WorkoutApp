<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.workoutapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText,GoogleAppIndexingWarning">
        <activity
            android:name=".UserSettingsActivity"
            android:label="@string/title_activity_user_settings"></activity>
        <activity android:name=".UserSettings" />
        <activity android:name=".Setup_UserInfo_activity" />
        <activity android:name=".DetailedMastery" />
        <activity android:name=".FillDetailsActivity" />
        <activity android:name=".Hub2Activity" />
        <activity
            android:name=".MasteryActivity"
            android:label="@string/title_activity_mastery"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>